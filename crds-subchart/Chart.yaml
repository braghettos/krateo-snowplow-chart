apiVersion: v2
name: krateo-snowplow-crd
description: Krateo PlatformOps Snowplow CRDs

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
# Pinned (NOT the CHART_VERSION tag): the snowplow restactions CRD versions
# independently of the snowplow app chart. The installer pins this snowplow-crd version.
# 0.21.0: schema refresh from the Ship S.1 / 0.30.205 generated CRD — adds the
# UserAccessFilter.resourcesFrom field + the resource/resourcesFrom XOR validation that
# the 1.0.x binary and the portal UAF RESTActions (compositions-list, etc.) require.
# Minor bump (additive, backward-compatible schema surface).
version: 0.21.4


home: https://krateo.io
icon: https://github.com/krateoplatformops/krateo/blob/main/docs/media/logo.svg
keywords:
  - krateo
  - platformops
  - snowplow
  - crd
sources:
  - https://github.com/braghettos/snowplow
